# QA 回归验证报告 · 里程碑模块 4 项用户反馈修复

| 项 | 内容 |
| --- | --- |
| 文档类型 | 回归测试报告（QA Regression Report） |
| 被测范围 | TASK-1 ~ TASK-4（delta-prd-milestone-fixes v1.0 / delta-design-milestone-fixes v1.0） |
| 测试负责人 | 严过关（QA 工程师） |
| 测试方式 | Vite SSR 加载**真实 TS 源码**做运行时断言 + 全量 typecheck / build + 源码事实核查 |
| 结论 | **IS_PASS = true（本次迭代范围内）**；ROUTE = **Engineer（1 项极轻微）** + **PM/team-lead（1 项越界发现，非本任务引入）** |

---

## 1. 实际执行的命令与脚本

全部在 `pm-app/web/` 下执行，**逐条真实运行**，非阅读代码推断：

| # | 命令 | 结果 |
| --- | --- | --- |
| 1 | `node scripts/qa_milestone_fixes.mjs`（本次新建，119 断言） | ✅ **119 / 119 通过，0 失败**（退出码 0） |
| 2 | `npm run typecheck`（`tsc --noEmit`） | ✅ 通过，零错误 |
| 3 | `npm run build`（`tsc --noEmit && vite build`） | ✅ 1537 modules transformed，4.72s，产物 1,107.80 kB（gzip 352.88 kB） |
| 4 | `node scripts/smoke_engine.mjs`（存量套件，交叉回归） | 104 断言 / 101 通过 / 3 失败（**全部为越界项，见 §5**） |
| 5 | `node scripts/qa_regression_simplify.mjs`（存量套件，交叉回归） | 48 断言 / 44 通过 / 4 失败（3 越界 + 1 新增轻微，见 §5） |

> 环境适配：两处存量脚本与新脚本的 Vite server 配置补 `optimizeDeps:{noDiscovery:true,include:[]}`，
> 避免 SSR 场景无谓的依赖预构建临时目录读写。不影响被测源码。

---

## 2. 纯函数断言结果（真实源码，非抄录）

### §1 `fitMilestoneDatesEx` / `fitMilestoneDates`（P0-M4 / SK-M7）— 29 / 29 ✅

- **S1 逐字命中 PRD P0-M4**：`planStart=2026-03-01`、`planEnd=2026-03-31`、A 类 offsets `0/30/70/125/170/210/232`
  → `03-01 / 03-05 / 03-10 / 03-17 / 03-23 / 03-28 / 03-31`，`compressed=true`、`stacked=false`、`ratio≈0.12931`、`planDays=30`、`templateSpan=232`。
- **规则 4 成立**：末碑恰好 `= planEnd`；**无任何碑晚于 planEnd**。
- S2 周期充足不压缩（`ratio=1`）；S3 `planDays = 碑数-1` 恰好逐日错开（`stacked=false`）；
  S4 极端周期封顶堆叠 `stacked=true` 且不越界；S5 零周期 / S6 全零 offset 全部落 `planStart`。
- **健壮性 B1~B7**：空 offsets 降级空数组、负周期按 0 处理不抛异常、NaN offset 不炸且非首碑保底 `planStart+1`、
  乱序 offsets **返回值与入参同索引对齐不错位**、薄封装一致、连续调用结果相同（无内部状态）、**不修改入参数组**。

### §2 `compareMilestones` / `sortMilestones`（SK-M1 / F-3 / F-4）— 7 / 7 ✅

- 三级键 `currentDate → createdAt → id(numeric)` 全部验证。
- **C3（F-4）**：同日同 `createdAt` 时 `P1-MS2` 正确排在 `P1-MS10` 之前（数字序而非字典序）——这正是 `createProject` 同批 `createdAt` 相同的场景。
- **C4（F-3 关键）**：故意注入脏 code（`M9` vs `M1`）**不影响比较结果**，证明已切断 `sort → code → sort` 循环依赖。
- 排序纯函数（不改原数组）、幂等（再排结果不变）。

### §3 `milestoneTaskDetail` 口径 Y（P0-M8 / P0-M9 / P0-M10）— 20 / 20 ✅

- **Y1→Y2 数字只增不减**：骨架未拆分 `total=1`；挂 4 个子任务后 `total=5`（**骨架不蒸发**，口径 X 的核心缺陷已消除）。
- `leaves=4`（仅真叶子）、`rollupIds=1` 且汇总节点正是骨架 W1。
- **Y4/Y5 加权正确性**：`progress=50`（4 叶子各 2 人天、2 个 100%）。
  反例校验：若误把 `estimateDays=0` 的骨架计入加权将得 40，实测非 40 → **0 工时骨架未参与加权、未稀释进度**。
- Y3 按 `wbsCode` 自然序（`1` 在 `1.1` 前、`1.2` 在 `1.10` 前）；Y7 父子同绑一碑按 id 去重不重复计数；
  Y8 多层子树只并入真叶子（中间层不入集），加权 `(3×100+1×0)/4 = 75`；Y9 不存在的碑返回空集合。

---

## 3. 六个剧本执行结论（A ~ F）

| 剧本 | 结论 | 关键证据（均为脚本运行时断言） |
| --- | --- | --- |
| **A 清空缓存 · 压缩 + 重排** | ✅ 通过（9/9） | 周期 `2026-03-01~03-31` 建 A 类项目 → 7 碑日期 **精确等于** `03-01/03-05/03-10/03-17/03-23/03-28/03-31`；编号 `M1..M7` 连续；列表顺序与日期同序；**无碑晚于 planEnd**；`baselineDate === currentDate`（创建即基线）；7 个 per-milestone 骨架；**骨架 description 内嵌 code 与重排后一致**（证明 renumber 早于骨架生成，触发点⑤到位）。 |
| **B 不清缓存 · 存量自愈（F-2）** | ✅ 通过（6/6） | 直接向持久化层注入脏 code `XX,M2,M9,M4,M5,M9,M7` → 读路径 `milestonesWithGate` **自动修复为连续编号且不再出现 M9**；**连刷 10 次编号完全稳定不抖动**（幂等）；另用**全新模块实例从 sessionStorage 还原**模拟真实刷新，注入的 `M42` 同样被自愈 → F-2 端到端成立。 |
| **C 插入 / 改期 / 重排（F-1）** | ✅ 通过（13/13） | 插入 `03-13` 新碑 → 编号 **M4**（不是 M8/M9），编号连续；末碑改到 `03-02` → **立即重排为 M2**，列表顺序与编号完全同步（「M8 排在 M2 上面」已消除）；删必备碑成功，剩余重排为连续 `M1..M7` 无空号；**删碑不删任务**（WBS 节点数不变），关联节点 `milestoneId` 置 null 仅解绑。 |
| **D 编辑两段式（P0-M7 / F-5）** | ✅ 通过（13/13） | **反例先立**：合并提交 `{name,target,延后日期}` 被 `E_MS_NEED_CHANGE` 拦截且**文本修改全部丢失** → 证明两段式是功能正确性前提、不可省。两段式实测：段 1 文本落库成功，段 2 日期延后被拦截并携带 `changeDraft`（含 changeType 与预填目标日期）；**重开编辑框：名称/目标为新值、日期为旧值**；仅提前日期一次成功且立即重排（M5→M2），顺序与编号仍同步；仅改文本不触发日期分支、编号不变。 |
| **E 计数与钻取同源（P0-M8/M9/M10）** | ✅ 通过（10/10） | 骨架挂 4 子任务 → 列表 `x/5`、`done=2`、`progress=50%`；**弹窗条目数 === 列表 total（SK-M5 铁律）**；骨架带「汇总」Chip 且汇总节点正是该骨架；4 条子任务为叶子；层级用于缩进（骨架 level=1 / 子任务 level=2）；**项目内全部里程碑逐一核对「钻取条目数 === 列表 total」全部通过**。 |
| **F 极端周期不阻断（P1-M12）** | ✅ 通过（8/8） | 周期 `2026-03-01~03-03` → `stacked=true`（向导据此出黄色告警）；**项目仍能成功创建，不阻断**；仍生成 7 碑；末碑 `= planEnd = 03-03`；无碑越界；允许同日堆叠；**同日堆叠下编号仍连续且连续读取不抖动**（三级 tie-break 兜底生效）。 |
| **附加 · 变更单回写重排（触发点④）** | ✅ 通过（6/6） | 变更实施后日期回写成功 → 自动重排为末位编号、编号连续、顺序与日期同序；**`baselineDate` 保持不变**（单向规则基石未被破坏）。 |

**P0-M1 五个写触发点 + 1 个读路径自愈，共 6 处，全部经运行时验证。**

---

## 4. 前端交付物源码核查（脚本不可达部分）

以下为纯 UI 层，脚本无法断言，已逐条核对源码事实：

| 需求 | 位置 | 核查结果 |
| --- | --- | --- |
| P0-M6 编辑入口 | `MilestonesPage.tsx:492-494` | ✅ 操作列铅笔 IconButton + Tooltip「编辑里程碑」，受 `editable` 权限约束 |
| P0-M7 两段式 | `MilestonesPage.tsx:182/186/194` | ✅ `textDirty` 先提 `{name,target}`、`dateDirty` 后提 `{currentDate}`；Toast 文案 **「名称与目标已保存；日期延后需提交变更单」** 与 PRD 逐字一致 |
| P0-M9/M10 钻取 | `MilestonesPage.tsx:765-766, 786` | ✅ 顶部「共 **N** 条关联任务；完成度按**叶子任务工时加权**计算，汇总节点不计入权重」；汇总节点带「汇总」Chip |
| P1-M13 删必备碑文案 | `MilestonesPage.tsx:692-696` | ✅ 必备碑专用文案（偏离规范 / 质量门一并删除 / WBS 仅解绑不删任务 / 不可撤销）；非必备碑走通用文案；`E_MS_REQUIRED_LOCKED` catch 分支保留（L232）作真实后端兼容 |
| P0-M5 周期重算 | `ProjectCreatePage.tsx:676, 699-701` | ✅ `periodDirty` 检测 + 提示条 +「按新周期重算日期」按钮（**不静默覆盖用户手改**） |
| P1-M12 堆叠告警 | `ProjectCreatePage.tsx:706, 712` | ✅ `compressed` / `stacked` 两级告警条 |

### 建议人工走查项（`npm run dev` 后确认视觉与交互）

引擎与文案均已验证，以下仅剩「肉眼观感」层面，建议 PM 走查一遍：

```bash
cd C:\Users\xuwen\WorkBuddy\AstrBytes\pm-app\web && npm run dev
```

1. 剧本 A/F：向导里改周期后告警条的**视觉层级与措辞是否够醒目**（黄色非阻断）。
2. 剧本 D：变更单弹窗在 Toast 之后弹出的**时序观感**（是否有闪烁 / 遮挡）。
3. 剧本 E：钻取弹窗**缩进层级在长列表下的可读性**。
4. 剧本 C：必备碑删除确认框中模板名当前为「该生命周期规范」，PRD 原文为「《{模板名}》」——**未做模板名插值**，属文案微差（见 K-2）。

---

## 5. 失败项归因（Smart Routing）

存量两套件共 7 条失败，逐条定性如下 —— **无一条属于 TASK-1~4 的功能性缺陷**：

### 5.1 已由我自行修复（ROUTE = QA，测试代码缺陷，Round 1 内闭环）

| # | 失败断言 | 根因 | 处理 |
| --- | --- | --- | --- |
| 1 | `smoke_engine`「必备碑 M1 锁删 → E_MS_REQUIRED_LOCKED」 | 用例停留在旧设计。PRD **P1-M13 已明确改为「允许删除必备碑 + 前端强化二次确认」** | 改为断言「必备碑可删除」+「删后编号重排连续」 |
| 2 | `smoke_engine`「删除自建碑后恢复原数量」 | #1 的级联位移 | 基线数量对齐修正 |
| 3 | `qa_regression_simplify`「新增碑编码 = M8」 | 用例期望旧的 `M{max+1}` 追加语义。**P0-M1 已改为按日期全量重排** | 改为断言「编号 = 其日期序位次」+「全表连续无空号」 |
| 4 | `qa_regression_simplify`「再增碑编码 = M9」 | 同上；且 **F-2 明确要求 M9 不得出现** | 改为断言「再增后编号仍连续」+「列表顺序与编号同序」 |

修复后：`smoke_engine` 101/104、`qa_regression_simplify` 44/48。

### 5.2 需工程师处理（ROUTE = Engineer · 极轻微）—— ✅ **已修复并复核关闭**

| 编号 | 问题 | 证据 | 严重度 | 状态 |
| --- | --- | --- | --- | --- |
| **E-1** | **术语禁用词回归**：`src/utils/wbs.ts` 引入禁用词「锚点」3 处 —— L134、L138、L166 | `qa_regression_simplify` G-G 全仓静态扫描由**基线零命中**变为 1 文件命中。禁用词表：`工作分区/工作包/生命周期阶段/归属阶段/阶段推进/锚点` | **低**（仅注释，非 UI 文案，无功能影响） | ✅ **CLOSED** |

**判定依据**：`milestoneTaskDetail` 是 TASK-4 新增函数，这 3 处注释随本次修改引入，而上一轮 QA 基线报告明确记录「全仓递归扫描零命中」。属本次迭代新增的规范违例。

**修复与 QA 复核（严过关独立执行，未采信工程师自述）**：

1. **超集扫描复验**：对 `src/` 递归搜索**全部 6 个**禁用词、**且不加文件扩展名过滤** → `No matches found`。
   G-G 段实现（`qa_regression_simplify.mjs` L370-387）为「递归 `src/` + 仅匹配 `.ts/.tsx/.vue/.js/.jsx` + 同样 6 词」，
   **QA 扫描范围是其严格超集且零命中 → G-G 断言必然为真**，该结论不依赖脚本执行。
2. **逻辑零改动确认**：通读 `wbs.ts` L149-178，口径 Y 三处要害原样保留 ——
   L152 `anchors = nodes.filter(n => n.milestoneId === milestoneId)`、
   L167 `collected.set(a.id, a)`（★ 骨架不蒸发的关键行）、
   `walkLeaves` 64 层成环兜底 + `compareWbsCode` 排序 + `rollupIds`/`leaves` 按 `parents` 划分。
   英文标识符 `anchors` 保留正确 —— 禁用词红线仅约束中文文案。
3. **影响面判定**：注释级文本替换，对运行时行为、类型、构建产物均无影响 → 119/119 与 typecheck 结论继续有效。

> ⚠️ **诚实声明**：E-1 关闭采用的是**静态等价核验**，而非重跑全量脚本。
> 原因：复核阶段 `node scripts/qa_regression_simplify.mjs` 被沙箱策略拦截（Bash 工具持续返回空输出），
> 工程师侧亦遇同一限制。上述第 1 条的超集扫描在逻辑上强于该断言本身，故判定可信；此处不做粉饰。

### 5.3 越界发现 · 需 PM / team-lead 决策（**非 TASK-1~4 引入，不派工程师**）

| 编号 | 问题 | 证据链 | 严重度 |
| --- | --- | --- | --- |
| **K-1** | **新建项目不再生成任何质量门**（`createProject` gates 恒为 0） | ① 两套件共 3+3 条门相关断言由绿转红（A 类期望 7 实得 0、C 类期望 5 实得 0、B 类期望 4 实得 0、`C-G4 未过门手工达成拒`因无门而失效）；② `git show HEAD:...index.ts` 证明**上一个提交 f5ed42c 中 `createProject` 是有 `db.gates.push` 的**；③ 现工作区全仓 `db.gates.push` **仅存在于种子 fixtures**（`fixtures/projects.ts:321`），业务路径已无建门代码 | **中** |

**为什么判定为「非本任务引入」**（关键，避免误伤工程师）：

1. 代码注释自述归属另一轮反馈：`index.ts:668`「**用户反馈②**：新建项目不再自动生成质量门」。同一轮「用户反馈②」还改了 L1005（必备碑不再锁删）、L1089（不再因质量门卡住达成）、L1172 与 `WbsPage.tsx:168/203`（子任务继承里程碑）——**这是一整轮独立的反馈迭代**。
2. 本任务的 `delta-prd-milestone-fixes.md` 中「反馈①②③④」= **①编号 ②默认碑超期 ③无法编辑 ④计数口径**，与质量门**完全无关**；`delta-design-milestone-fixes.md` 对 gate 的指示是 **`gate: /* 保持不变 */` / `/* 原样 */`**（L851、L876），即明令不动。
3. 因此该改动落在「上一轮 simplify 交付之后、本轮里程碑修复之前（或并行）」的未提交工作区中。

**遗留风险（请 PM 裁决，不阻断本次交付）**：

- `createProject` 内 `templateSpecs` 仍在构造 `gate` 对象（`index.ts:626-633`）却**从未被消费** → **死代码**。
- 建项审计日志会写「…生成 N 个里程碑、**0 道质量门**」（`index.ts:740`），**用户可见的措辞矛盾**。
- 删除确认文案仍写「其质量门一并删除」（`MilestonesPage.tsx:692`），但新项目根本没有门 → **文案与事实不符**。
- 段落注释 `/* 里程碑 + 内联质量门 */`（`index.ts:606`）已过期。
- 项目列表 `gateTotal` 对所有新项目恒为 0，质量门功能对新项目**事实性下线**（种子演示项目仍有门，故页面不会报错，问题被掩盖）。

> 我**刻意不把这 6 条门断言改成期望 0**。若默默改绿，就等于把一个可能非预期的产品下线动作固化进回归基线。保持红色是为了让这个决策显式化。

### 5.4 沿用上一轮的加固建议（未复现为失败，仅记录）

- **K-2 文案微差**：P1-M13 要求「偏离《{模板名}》生命周期规范」，实现为「偏离该生命周期规范」，**未插值模板名**。极轻微，可选修。

---

## 6. 覆盖率与测试设计说明

- **断言总量**：本次新增 119 条（全绿）+ 存量交叉回归 152 条 = **271 条**。
- **覆盖策略**：全部打在**真实生产代码路径**上（Vite SSR 直载 `.ts` 源码），不抄录、不 mock 业务逻辑，因此断言通过 == 生产行为正确。
- **「不盲信」的落实**：工程师自报的 6 个 renumber 触发点，我没有读代码确认，而是**分别构造运行时场景逐个触发并验证编号结果**（建碑/删碑/改期/变更单回写/建项/读路径）。
- **反例设计**：D2（合并提交丢文本）与 Y5（骨架计入加权得 40）两条**反向断言**，用于证明「修复是必要的」而不仅是「修复后能跑」。
- **F-2 的真实性**：不满足于同进程调用，额外**启第二个 Vite 实例、用全新模块图从 sessionStorage 还原**，逼近真实浏览器刷新。

---

## 7. 交付物

- 新增测试脚本：`pm-app/web/scripts/qa_milestone_fixes.mjs`（119 用例，可复跑，退出码即结论）
- 更新测试脚本：`pm-app/web/scripts/smoke_engine.mjs`、`pm-app/web/scripts/qa_regression_simplify.mjs`（对齐新 PRD 的 4 条陈旧断言）
- 本报告：`pm-app/docs/qa_regression_report_milestone_fixes.md`

---

## 8. 最终签核

- **本次迭代范围（TASK-1~4 / 反馈①②③④）：IS_PASS = true**
  编号重排、等比压缩、三字段编辑、计数口径四条主线**全部经运行时验证通过**；typecheck 与 build 全绿。
- **ROUTE = NoOne（本次迭代范围内已清零）**
  原 **E-1**（`wbs.ts` 注释禁用词「锚点」×3）已由工程师修复，并经 QA **超集扫描 + 逐行逻辑核对**独立复核关闭（见 §5.2）。
- **ROUTE = PM / team-lead（范围外，仍待裁决）**：**K-1**（新建项目零质量门）——**非本任务引入**，但已造成死代码 + 3 处用户可见文案矛盾，需产品明确「质量门是否正式下线」，再据此统一清理代码、文案与回归基线。
- 里程碑模块 4 项用户反馈修复**独立回归通过，可进入合流 / 发布评审**。
- **收敛轮次**：Round 1 完成主体验证并定位 E-1；Round 2 完成 E-1 修复复核并关闭。**未超两轮上限。**
