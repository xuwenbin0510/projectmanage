# 依赖与构建产物（容器内重新安装/构建）
node_modules
web/node_modules
web/dist

# 密钥与本地数据（绝不进镜像，运行时通过 env_file / volume 注入）
.env
*.db
*.db-shm
*.db-wal
attachments

# 仓库/工具杂项
# ⚠ .git 必须进构建上下文：构建期要跑 `git rev-parse --short HEAD` 生成左下角版本号。
#   镜像内会在构建结束时删除它（见 Dockerfile），不会随产物发布。
.cache
.workbuddy
pm-app-deploy*
*.log
